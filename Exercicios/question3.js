//Gere um novo array com cada um dos numero elevado ao quadrado do array abaixo.

const numbers = [5, 7, 3];

const squareNumbers = numbers.map(number => number*number); //Ao quadrado = number*number || number ** 2

console.log(squareNumbers);